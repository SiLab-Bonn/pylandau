language: python
python:
  - 2.6
  - 2.7
  - 3.2
  - 3.3
  - 3.4

sudo: false

notifications:
  email:
    - pohl@physik.uni-bonn.de

# Install
install:
  - pip install -r requirements.txt
  - python setup.py install

# Run test
script: nosetests
